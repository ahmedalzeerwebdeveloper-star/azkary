# تعليمات صارمة للمشروع (Strict Rules)

1. **التحقق من الفيرجن قبل كل بناء (Version Bump Check)**:
   - قبل تنفيذ أي أمر بناء للتطبيق (`flutter build apk` أو `flutter build appbundle`)، يجب حتماً التحقق من زيادة رقم الإصدار ورقم البناء في `pubspec.yaml` (مثال: `version: x.y.z+n` تزيد إلى `version: x.y.(z+1)+(n+1)`).

2. **بناء التطبيق للمتجر وعدم إنهاء المهمة بدون البناء**:
   - لا تنتهي المهمة إلا بعد بناء حزمة التطبيق الموجهة للمتجر `flutter build appbundle --release` والتأكد من نجاح البناء وتوليد ملف `app-release.aab` بتاريخ ووقت حديث.

3. **حفظ ومزامنة التعديلات عبر Git**:
   - بعد الانتهاء من العمليات والبناء بنجاح، يجب عمل commit للتعديلات برسالة واضحة ودقيقة باللغة العربية ثم عمل `git push` إلى المستودع.
